# Docker environment template — safe to commit (no real secrets)
# Copy to .env and fill in real values before running docker-compose

JWT_SECRET=change_me_before_running
MASTER_KEY=change_me_before_running
SENDGRID_KEY=placeholder_update_if_needed
